# recipe — CONTEXT (유일한 진입점)

> 새 세션은 이 문서를 읽는 것으로 시작한다. 이 문서는 "확정 결정"만 담는다.
> 작업 로그·진행 상황은 여기에 쓰지 않는다 (아래 지침 참고).

## 0. AI 작업 지침 (매 세션 필수)
- 작업 전: `docs/recipe/progress.html`의 "다음 세션이 이어서 할 일"을 확인하고 그 작업부터 진행한다.
- 작업 후(또는 중단 시): `progress.html`의 체크리스트, "다음 세션이 이어서 할 일", 갱신일을 갱신한다.
  진행 상태의 유일한 원본은 progress.html이다. 이 문서(CONTEXT.md)에 상태를 중복 기록하지 않는다.
- 이 문서는 설계 결정이 바뀔 때만 수정한다.
- progress.html의 "지금 알아야 할 것"에는 사용자 액션이 필요한 항목을 항상 최상단에 유지한다.
- 쓰기·삭제·편집·생성 툴은 사용자 응답에 '승인' 문자열이 포함된 경우에만 사용한다 (프로젝트 공통 규칙).

## 1. 비전과 단계
- 문제: 쇼츠(요리·생활팁)를 재생목록에 저장해두고 필요한 순간에 꺼내지 못한다("저장해놓고 까먹어야지").
- 장기 비전: 저장한 쇼츠를 필요한 순간에 꺼내주는 앱.
- 1단계(현재): 요리 영역부터. 요리는 "뭐 해먹지"라는 매일 반복되는 트리거와
  냉장고 재고라는 검색 조건이 있어 '꺼내기'가 실제로 작동하는 첫 영역이다.
- 2단계(실사용 검증 후): Android 앱 전환 + 영역 확장 + 중앙 분석 DB 공유
  (영상 ID당 분석 1회, 사용자 개인 DB는 영상 ID + 개인 데이터만 보유).

## 2. 확정 설계 결정 (2026-07-08 인터뷰)

### 플랫폼·저장
- 형태: 모바일 웹앱. S25 홈화면에 추가해 앱처럼 사용. mac-mini의 home 서비스에서 제공.
- 위치: home 프로젝트에 recipe 도메인으로 추가 (nexus에 넣지 않음 — nexus 비대화 방지).
- DB: PostgreSQL에 recipe 전용 데이터베이스 신설. 기존 DB와 완전 분리.

### 레시피
- 등록: 유튜브 재생목록(일부공개) URL 일괄 등록 + 이후 링크 개별 추가. 자동 동기화 없음.
- 추출: Gemini API에 유튜브 URL 직접 전달 (영상 다운로드 안 함).
  - 이유: 말 없는 쇼츠(배경음악+화면자막)도 화면을 보고 추출 가능,
    유튜브 IP 차단 문제 원천 해소(구글이 영상 접근), 로컬 gemma 품질 문제 회피.
  - 무료 등급 사용 (카드 등록 없이). 해당 구글 프로젝트에 결제 연결 금지(무료 등급 소멸됨).
  - 일괄 등록은 대기열 방식: 분당 한도에 맞춰 순차 분석, 429 시 대기 후 자동 재개, 진행률 표시.
  - 페일오버로 로컬 모델을 쓰지 않는다(품질 낮은 데이터가 남는 관리 문제). 기다렸다 재시도만.
- 저장 필드: 이름 / 재료 태그 / 조리시간(분) / 단계 요약 / 원본 링크.
  조리 단계는 요약만 저장, 상세는 원본 영상으로 확인.
- 고유 열쇠 = 유튜브 영상 ID (중복 등록 방지 + 훗날 중앙 DB 공유의 기반).

### 재료 사전 (태그의 뼈대)
- 추출된 재료명을 표준 이름으로 정규화 (계란=달걀, 파/대파 통일).
- 분류: 주재료 / 기본양념 / 특이양념.
  - 기본양념(소금·간장 등 오너 부엌 상비 ~20개): 매칭 판정에서 제외.
  - 특이양념(굴소스 등): 판정에서 제외하되 추천 결과에 "OO 필요" 경고 표시.
  - 새 재료 발견 시 미분류로 넣고 사용자에게 분류를 물어봄. 기준은 "오너 부엌에 상비인가"(AI가 알 수 없는 정보).
- 재료별 "추가 횟수"를 기록 (자주 사는 재료 버튼의 근거).

### 냉장고 (재고)
- 있다/없다만 관리. 수량·유통기한 입력 없음 (입력 부담이 기능 생존의 최대 변수).
- 등록일 자동 기록. 같은 재료 재등록 시 등록일을 오늘로 갱신 (리필 대응).
- 화면: 오래된 순 목록. 항목별 삭제(확인 없이 즉시)·임박 토글(수동 지정)·수정 버튼.
- 수정: 이름 교정 + 등록일 변경 (뒤늦은 등록 대응).
- 추가: 자주 사는 재료 상위 12개 버튼 + 자유 입력. 카테고리 트리 탐색은 만들지 않는다.

### 추천
- 2층 구조: 1층 "지금 만들 수 있어요" 최대 5개 / 2층 "하나만 사면 돼요" 최대 3개(+살 재료 표시).
- 정렬: 임박 재료 사용 레시피 최상단 → 그다음 오래된 재료 사용 순.
- 계산: 재료 태그 집합 비교. LLM 사용 안 함 (추천은 비용 0, 즉시 응답).
- 상세: 재료 + 요약 단계 + 원본 영상 링크.

## 3. 분리 규율 (앱 전환 대비 — 반드시 지킬 것)
1. 코드 격리: 백엔드는 `domain/recipe` 패키지 안에서만. 다른 도메인과 상호 참조 금지.
   프론트는 `frontend/src/domain/recipe/`에 격리.
2. DB 격리: recipe 전용 데이터베이스 + 전용 접속 설정. 기존 테이블과 외래키·조인 금지.
3. API 격리: 경로 `/api/recipe/**` 통일.
4. AI 호출 격리: Gemini 호출을 인터페이스 뒤에 숨김 (URL 프리뷰 정책 변경 시 구현체만 교체).
5. 설정 격리: recipe 관련 설정(API 키 등)은 별도 설정 그룹으로.

## 4. 구현 순서 (각 단계 완료 시 폰에서 실사용 가능해야 함)
1차: DB 생성 + 재료 사전 + 냉장고 화면 (추가/삭제/임박/수정)
2차: Gemini 추출 (영상 1개 등록) — 추출 품질 검증 지점
3차: 재생목록 일괄 등록 (대기열)
4차: 추천 화면 (2층 추천 + 상세)
5차: 다듬기 (자주 사는 재료 버튼, 새 재료 분류 질문, 진행률 표시)

## 5. 리스크 (인지한 상태로 진행)
- Gemini URL 기능은 프리뷰: 정책 변경 시 yt-dlp 다운로드+업로드 방식으로 교체 (규율 4로 대비).
- 추출 품질 미검증: 2차에서 검증. 부족하면 프롬프트·해상도 조정 → 그래도 안 되면 유료 모델 소액(영상당 10~15원) 재논의.
- 유튜브가 유사 기능 출시 가능성: 개인 도구 가치는 불변. 상용화 판단 시점에 재평가.
- 무료 등급 한도: 일 약 250~1,500요청, 분당 약 10요청, 유튜브 URL 일 8시간 분량.
