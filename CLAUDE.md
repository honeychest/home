@AGENTS.md

<!-- 규칙의 원본은 AGENTS.md 한 곳이다 (Codex 등 다른 에이전트도 그 파일을 읽는다).
     여기에 규칙을 직접 쓰지 말 것 — 두 벌이 되어 서로 어긋난다.
     주의: `npx gitnexus analyze` 가 이 파일에도 gitnexus 블록을 다시 붙일 수 있다.
     그 경우 AGENTS.md 쪽 블록만 남기고 이 파일에서는 지운다. -->
